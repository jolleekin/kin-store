#!/bin/sh
deno lint || exit 1

# Only pay for the full test run when a commit actually touches library code.
if git diff --cached --name-only | grep -qE '^(core|plugins|react)/.*\.tsx?$'; then
  deno test -A || exit 1
fi
